import { Typography } from '../../components/ui/Typography/Typography.tsx';

export function NotFound() {
  return (
    <Typography tagName={'h1'} center variant={'l'}>
      Page not Found
    </Typography>
  );
}
